class Solution:
    def removeDuplicateLetters(self, s: str) -> str:
        unique = set()
        for c in s:
            unique.add(c)
        unique = sorted(unique)
        ans = ""
        
        def valid(char):
            
            
        while len(unique) > 0:
            if valid(candidate):
                ans += candidate
                
        
        
        